import React from "react";
import { InputBase as CustomInputBase } from "@mui/material";

const InputBase = ({ placeholder }) => {
  return <CustomInputBase placeholder={placeholder}></CustomInputBase>;
};

export default InputBase;
