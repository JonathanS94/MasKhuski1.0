import React from "react";
import { Facebook as CustomFacebookIcon } from "@mui/icons-material";
import IconButton from "./IconButton";

const FacebookIcon = ({}) => {
  return (
  <IconButton>
    <CustomFacebookIcon/>
  <IconButton/> );
};

export default FacebookIcon;
