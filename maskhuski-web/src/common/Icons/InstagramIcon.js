import React from "react";
import { Instagram as CustomInstagramIcon } from "@mui/icons-material/";
import IconButton from "./IconButton";

const InstagramIcon = ({}) => {
  return (
    <IconButton>
      <CustomInstagramIcon />
    </IconButton>
  );
};

export default InstagramIcon;
