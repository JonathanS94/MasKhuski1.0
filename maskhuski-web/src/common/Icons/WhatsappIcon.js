import React from "react";
import { WhatsApp as CustomWhatsAppIcon } from "@mui/icons-material/";
import IconButton from "./IconButton";

const WhatsAppIcon = ({}) => {
  return (
    <IconButton>
      <CustomWhatAppIcon />
    </IconButton>
  );
};

export default WhatsAppIcon;
