import React from "react";
import { Search as CustomSearchIcon } from "@mui/icons-material/";
import { IconButton } from "@mui/material";

const SearchIcon = ({}) => {
  return (
    <IconButton>
      <CustomSearchIcon />
    </IconButton>
  );
};

export default SearchIcon;
